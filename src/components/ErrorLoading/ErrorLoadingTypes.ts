/**
 * Props for the ErrorLoading component.
 *
 * @interface ErrorLoadingProps
 * @property {string} message - The error message to be displayed in the ErrorLoading component.
 */
export interface ErrorLoadingProps {
    message: string;
}